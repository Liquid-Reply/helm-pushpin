apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pushpin-chart.fullname" . }}-test-connection"
  labels:
    {{- include "pushpin-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pushpin-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
